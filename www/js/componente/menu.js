angular.module('menuInicial', [])
	.service('loadInicial', function() {
		this.carregar = function() {
			// Metodo Construtor compartilhado em todas as paginas.
			// Principalmente para caso haja menus com efeitos.
		}
	});